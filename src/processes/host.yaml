version: "1.0"
namespace: app.processes

entries:
  - name: host
    kind: process.host
    host:
      workers: 4
      queue_size: 1024
      local_queue_size: 256
    lifecycle:
      auto_start: true
